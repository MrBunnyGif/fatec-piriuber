import React, { Fragment } from 'react'
import PreviousButton from '../../../components/PreviousButton'

export default () => {
	return (
		<Fragment>
			<PreviousButton />
			<h1>Driver list</h1>
		</Fragment>
	)
}